{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for DFtool windows",
  "windows": ["control", "overlay"],
  "permissions": ["core:default", "opener:default", "global-shortcut:default"]
}
